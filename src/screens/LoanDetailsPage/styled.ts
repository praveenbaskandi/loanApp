import { StyleSheet } from 'react-native';

export const loanDetailStyles = StyleSheet.create({
  container: {
    padding: 20,
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 50,
  },
  title: { fontSize: 18, fontWeight: 'bold', marginBottom: 8 },
});
